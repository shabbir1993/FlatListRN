Flat List component implementation in React Native.

- `git clone https://github.com/shabbir1993/FlatListRN.git`
- `cd FlatListRN/`
- `npm install`
- `react-native run-ios` or `react-native run-android`
